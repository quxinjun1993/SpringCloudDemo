server:
  port: 9000

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/
spring:
  application:
    name: zuul

zuul:
  routes:
    userService: /user/**
    payService: /pay/**
    orderService:  /order/**


